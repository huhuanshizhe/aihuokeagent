# DIW CSV must be a normal Git blob so Vercel/Docker include the real file
# (not an LFS pointer). Keep other cache CSVs on LFS if added later.
data/cache/*.csv filter=lfs diff=lfs merge=lfs -text
data/cache/thailand-factories.csv -filter -diff -merge
